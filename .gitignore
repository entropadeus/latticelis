# Build artifacts
.build-cache/
node_modules/
dist/
ui-audit-shots/
vendor/

# Local Electron packaging experiment
electron/
package.json
package-lock.json
scripts/sync-renderer-vendor.cjs

# Internal / AI scaffolding
.claude/
_design/

# Internal docs (not for public deployment)
# Keep all markdown private except the README.
*.md
!README.md
# Internal text notes (build ledger, scratchpads, etc.)
*.txt
# Exception: the build ledger is the cloud build agent's source of truth.
# It needs to live in the repo so a fresh clone can read it. Other .txt
# files (scratchpads, drafts) stay gitignored.
!Lattice LIS — Build Ledger.txt

# OS junk
.DS_Store
Thumbs.db
